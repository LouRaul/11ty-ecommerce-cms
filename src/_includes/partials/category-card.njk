<div class="category-card">
  <img src="{{ category.data.image }}" alt="{{ category.data.name }}" class="w-full h-48 object-cover">
  <div class="p-4">
    <h3 class="text-xl font-semibold">{{ category.data.name }}</h3>
    <p class="text-gray-600">{{ category.data.description }}</p>
  </div>
</div> 